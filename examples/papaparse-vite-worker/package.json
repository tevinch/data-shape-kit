{
  "name": "papaparse-vite-worker-check",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "test": "node --test test.mjs",
    "build": "vite build",
    "build:default": "vite build --minify oxc --outDir dist-default",
    "build:unminified": "vite build --minify false --outDir dist-unminified",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "papaparse": "5.7.0"
  },
  "devDependencies": {
    "vite": "8.2.2",
    "terser": "5.51.2"
  }
}
