diff --git a/node_modules/@tiptap/extension-code/src/code.ts b/node_modules/@tiptap/extension-code/src/code.ts
index 1f317bf..0e11be9 100644
--- a/node_modules/@tiptap/extension-code/src/code.ts
+++ b/node_modules/@tiptap/extension-code/src/code.ts
@@ -93,6 +93,18 @@ export const pasteRegexMatch = (text: string): PasteRuleMatch[] => {
   return matches
 }
 
+// A code span must be fenced by a backtick run its own content does not contain.
+function shortestUnusedBacktickRun(text: string): number {
+  const runs = new Set((text.match(/`+/g) || []).map(run => run.length))
+  let length = 1
+
+  while (runs.has(length)) {
+    length += 1
+  }
+
+  return length
+}
+
 /**
  * This extension allows you to mark text as inline code.
  * @see https://tiptap.dev/api/marks/code
@@ -128,12 +140,19 @@ export const Code = Mark.create<CodeOptions>({
     return helpers.applyMark('code', [{ type: 'text', text: token.text || '' }])
   },
 
-  renderMarkdown: (node, h) => {
+  renderMarkdown: (node, h, context) => {
     if (!node.content) {
       return ''
     }
 
-    return `\`${h.renderChildren(node.content)}\``
+    // Children are a placeholder here, so choose the fence from the full marked text.
+    const markText = typeof context?.meta?.markText === 'string' ? context.meta.markText : ''
+    const fence = '`'.repeat(shortestUnusedBacktickRun(markText))
+    const edgeBacktick = markText.startsWith('`') || markText.endsWith('`')
+    const bothEdgeSpaces = markText.startsWith(' ') && markText.endsWith(' ') && !/^ +$/.test(markText)
+    const padding = edgeBacktick || bothEdgeSpaces ? ' ' : ''
+
+    return `${fence}${padding}${h.renderChildren(node.content)}${padding}${fence}`
   },
 
   addCommands() {
diff --git a/node_modules/@tiptap/extension-code/dist/index.js b/node_modules/@tiptap/extension-code/dist/index.js
index 4309e96..e8b4da8 100644
--- a/node_modules/@tiptap/extension-code/dist/index.js
+++ b/node_modules/@tiptap/extension-code/dist/index.js
@@ -46,6 +46,12 @@ const pasteRegexMatch = (text) => {
 	}
 	return matches;
 };
+function shortestUnusedBacktickRun(text) {
+	const runs = new Set((text.match(/`+/g) || []).map((run) => run.length));
+	let length = 1;
+	while (runs.has(length)) length += 1;
+	return length;
+}
 /**
 * This extension allows you to mark text as inline code.
 * @see https://tiptap.dev/api/marks/code
@@ -75,9 +81,15 @@ const Code = Mark.create({
 			text: token.text || ""
 		}]);
 	},
-	renderMarkdown: (node, h) => {
+	renderMarkdown: (node, h, context) => {
 		if (!node.content) return "";
-		return `\`${h.renderChildren(node.content)}\``;
+		var _context$meta;
+		const markText = typeof (context === null || context === void 0 || (_context$meta = context.meta) === null || _context$meta === void 0 ? void 0 : _context$meta.markText) === "string" ? context.meta.markText : "";
+		const fence = "`".repeat(shortestUnusedBacktickRun(markText));
+		const edgeBacktick = markText.startsWith("`") || markText.endsWith("`");
+		const bothEdgeSpaces = markText.startsWith(" ") && markText.endsWith(" ") && !/^ +$/.test(markText);
+		const padding = edgeBacktick || bothEdgeSpaces ? " " : "";
+		return `${fence}${padding}${h.renderChildren(node.content)}${padding}${fence}`;
 	},
 	addCommands() {
 		return {
@@ -113,5 +125,3 @@ const Code = Mark.create({
 var src_default = Code;
 //#endregion
 export { Code, src_default as default, inputRegex, inputRegexMatch, pasteRegex, pasteRegexMatch };
-
-//# sourceMappingURL=index.js.map
\ No newline at end of file
diff --git a/node_modules/@tiptap/extension-code/dist/index.cjs b/node_modules/@tiptap/extension-code/dist/index.cjs
index 5643725..43c992b 100644
--- a/node_modules/@tiptap/extension-code/dist/index.cjs
+++ b/node_modules/@tiptap/extension-code/dist/index.cjs
@@ -50,6 +50,12 @@ const pasteRegexMatch = (text) => {
 	}
 	return matches;
 };
+function shortestUnusedBacktickRun(text) {
+	const runs = new Set((text.match(/`+/g) || []).map((run) => run.length));
+	let length = 1;
+	while (runs.has(length)) length += 1;
+	return length;
+}
 /**
 * This extension allows you to mark text as inline code.
 * @see https://tiptap.dev/api/marks/code
@@ -79,9 +85,15 @@ const Code = _tiptap_core.Mark.create({
 			text: token.text || ""
 		}]);
 	},
-	renderMarkdown: (node, h) => {
+	renderMarkdown: (node, h, context) => {
 		if (!node.content) return "";
-		return `\`${h.renderChildren(node.content)}\``;
+		var _context$meta;
+		const markText = typeof (context === null || context === void 0 || (_context$meta = context.meta) === null || _context$meta === void 0 ? void 0 : _context$meta.markText) === "string" ? context.meta.markText : "";
+		const fence = "`".repeat(shortestUnusedBacktickRun(markText));
+		const edgeBacktick = markText.startsWith("`") || markText.endsWith("`");
+		const bothEdgeSpaces = markText.startsWith(" ") && markText.endsWith(" ") && !/^ +$/.test(markText);
+		const padding = edgeBacktick || bothEdgeSpaces ? " " : "";
+		return `${fence}${padding}${h.renderChildren(node.content)}${padding}${fence}`;
 	},
 	addCommands() {
 		return {
@@ -122,5 +134,3 @@ exports.inputRegex = inputRegex;
 exports.inputRegexMatch = inputRegexMatch;
 exports.pasteRegex = pasteRegex;
 exports.pasteRegexMatch = pasteRegexMatch;
-
-//# sourceMappingURL=index.cjs.map
\ No newline at end of file
