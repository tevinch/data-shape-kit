{
  "name": "work-calendar-example",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs",
    "example": "npm run build && node example.mjs"
  },
  "devDependencies": {
    "@date-fns/tz": "1.5.0",
    "@types/node": "24.13.4",
    "date-fns": "4.4.0",
    "typescript": "5.9.3"
  }
}
